<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Portfolio - Dark/Light Mode</title>

    <style>
        /* ==================== Theme Variables ==================== */
        :root {
            --bg-color: #f4f4f4;
            --text-color: #222;
            --accent-color: #0077cc;
            --card-bg: #fff;
            --navbar-bg: #0077cc;
            --navbar-text: #fff;
            --profile-link-bg: #24292e;
            --profile-link-text: #fff;
        }

        body.dark-mode {
            --bg-color: #1e1e1e;
            --text-color: #e0e0e0;
            --accent-color: #00bfff;
            --card-bg: #2c2c2c;
            --navbar-bg: #121212;
            --navbar-text: #fff;
            --profile-link-bg: #444;
            --profile-link-text: #fff;
        }

        /* ==================== Global Styles ==================== */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            line-height: 1.6;
            background-color: var(--bg-color);
            color: var(--text-color);
            transition: background 0.3s ease, color 0.3s ease;
        }

        /* ==================== Navbar ==================== */
        #navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background-color: var(--navbar-bg);
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            z-index: 1000;
        }

        #navbar a {
            color: var(--navbar-text);
            text-decoration: none;
            margin-right: 20px;
            font-weight: bold;
        }

        #navbar a:hover {
            text-decoration: underline;
        }

        /* ==================== Theme Toggle Button ==================== */
        #theme-toggle {
            padding: 8px 12px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            background-color: var(--accent-color);
            color: #fff;
            font-weight: bold;
            transition: background 0.3s ease;
        }

        #theme-toggle:hover {
            filter: brightness(85%);
        }

        /* ==================== Sections ==================== */
        section {
            padding: 120px 20px 60px 20px;
            max-width: 1000px;
            margin: 0 auto;
        }

        /* ==================== Welcome Section ==================== */
        #welcome-section {
            background: linear-gradient(135deg, var(--accent-color), #00c6ff);
            color: #fff;
            text-align: center;
            border-radius: 12px;
            transition: background 0.3s ease;
        }

        #welcome-section h1 {
            font-size: 48px;
            margin: 0;
            padding: 60px 20px;
        }

        #welcome-section p {
            font-size: 18px;
            margin: 20px 0;
        }

        #profile-link {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 15px;
            background-color: var(--profile-link-bg);
            color: var(--profile-link-text);
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            transition: background 0.3s ease;
        }

        #profile-link:hover {
            filter: brightness(85%);
        }

        /* ==================== Projects Section ==================== */
        #project-section h2 {
            text-align: center;
            margin-bottom: 40px;
            font-size: 36px;
        }

        .project-tile {
            background-color: var(--card-bg);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .project-tile:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }

        .project-tile h3 {
            margin-top: 0;
            margin-bottom: 10px;
            color: var(--accent-color);
        }

        .project-tile a {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: bold;
        }

        .project-tile a:hover {
            text-decoration: underline;
        }

        /* ==================== Responsive ==================== */
        @media (max-width: 700px) {
            #welcome-section h1 {
                font-size: 36px;
            }

            #navbar {
                flex-direction: column;
                align-items: flex-start;
            }

            #navbar a {
                margin-bottom: 10px;
            }

            .project-tile {
                padding: 15px;
            }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav id="navbar">
        <div>
            <a href="#welcome-section">Welcome</a>
            <a href="#project-section">Projects</a>
        </div>
        <button id="theme-toggle">Toggle Theme</button>
    </nav>

    <!-- Welcome Section -->
    <section id="welcome-section">
        <h1>Welcome to My Portfolio</h1>
        <p>Hi! I’m Royce, a web developer passionate about creating modern web applications.</p>
        <a id="profile-link" href="https://github.com/royce" target="_blank">My GitHub Profile</a>
    </section>

    <!-- Projects Section -->
    <section id="project-section">
        <h2>Projects</h2>

        <div class="project-tile">
            <h3>Portfolio Website</h3>
            <p>A responsive portfolio website built using HTML, CSS, and JavaScript.</p>
            <a href="https://github.com/royce/portfolio" target="_blank">View Project</a>
        </div>

        <div class="project-tile">
            <h3>Todo App</h3>
            <p>A full-featured Todo App using localStorage and modern JavaScript features.</p>
            <a href="https://github.com/royce/todo-app" target="_blank">View Project</a>
        </div>

    </section>

    <!-- ==================== Script for Dark/Light Toggle ==================== -->
    <script>
        const toggleButton = document.getElementById('theme-toggle');
        toggleButton.addEventListener('click', () => {
            document.body.classList.toggle('dark-mode');
        });
    </script>

</body>
</html>
